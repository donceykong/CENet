  Run for KITTI-360:
  python analyze_edl_uncertainty_accuracy.py --dataset kitti360_0000

  Or for MCD (same as before but now with common labels):
  python analyze_edl_uncertainty_accuracy.py --dataset mcd_kth_night
